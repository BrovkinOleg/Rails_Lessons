<h1>Passing the "<%= @test_passage.test.title %>" Test</h1>

<b> Choose answer for question "<%= @test_passage.current_question.body %>"</b><br>

<b> Question number <%= @test_passage.current_question_number %> of <%= @test_passage.questions_number %></b>

<%= form_with url: test_passage_path(@test_passage), local: true, method: :put do |form| %>
  <%#= form.collection_check_boxes :answer_ids, @test_passage.current_question.answers, :id, :body, include_hidden: false do |b| %>
  <%= form.collection_check_boxes :answer_ids, @test_passage.current_question.answers, :id, :body do |b| %>
    <p>
      <%= b.check_box id: "answer_ids_#{b.object.id}" %>
      <%= b.label %>
    </p>
  <% end %>
  <p><%= form.submit 'Next' %></p>
<% end %>
